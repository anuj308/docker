Task 12 - MySQL Container Debugging

Correct command:
docker run -d --name mysql_debug -e MYSQL_ROOT_PASSWORD=rootpass -e MYSQL_DATABASE=college_db -e MYSQL_USER=college_user -e MYSQL_PASSWORD=college_pass -p 3307:3306 mysql:8

Debug commands:
docker logs mysql_debug
docker exec -it mysql_debug mysql -u root -prootpass -e "SHOW DATABASES;"

Cleanup commands:
docker stop mysql_debug
docker rm mysql_debug

Status:
The mysql:8 image exists locally, but this real run could not be completed after the interruption because the Docker daemon became unreachable. Before the interruption, other real Docker tasks were successfully run and verified.
